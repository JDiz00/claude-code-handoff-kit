<!-- This is a REAL handoff generated by the /handoff skill on a small demo repo
(Claude Code 2.1.163, Claude Sonnet). Only the username in paths was redacted.
Note the mistake sweep in State/Next: the session found uncommitted changes it did
not make, and flagged them instead of touching them. -->

# Handoff — demo-repo — status: ready
timestamp: 2026-07-17 06:53 CDT
cwd: /home/alex/projects/handoff-kit/demo-repo
repo_root: /home/alex/projects/handoff-kit/demo-repo
branch: master
head: e911e9d
dirty: staged=0 unstaged=1 untracked=2

RESUME RITUAL: compare repo_root+branch+head+dirty against live `git status`; report
any mismatch BEFORE changing files. Rerun Evidence only if the command is safe. Then
state Goal/State/Next in 3 lines and start Next 1 in the same turn unless blocked.

## Goal
demo-app: tiny todo CLI used to demo the Claude Code Handoff Kit (per README.md). No
active task given this session beyond producing this checkpoint.

## State
No work was done this session — this handoff checkpoints pre-existing working-tree
state, not anything produced this session. Since the prior handoff (06:52, clean
tree), the tree picked up: README.md gained an appended line "# scratch note", and
an untracked notes.json (`{"todo": true}`) appeared. Origin/intent of both is
unknown — neither was written by this session's actions.

## Evidence
`git status --porcelain` → `M README.md`, `?? .claude/`, `?? notes.json`.
`git diff README.md` → added line `# scratch note`. No tests run, no code executed.

## Runtime
None — no servers, processes, or background jobs running.

## Next
1. Ask the user whether the README "# scratch note" line and notes.json
   (`{"todo": true}`) are intentional/wanted, or should be reverted/removed —
   don't touch without confirmation since their origin is unclear.
2. If notes.json is meant to stay, clarify its purpose (currently just a stray flag).
3. No other prior direction given this session — otherwise wait for user direction
   on demo-app / todo_cli.py.

## Tried / constraints
None — this session made no attempts at anything.

## Decisions
None made this session.

## Key files
- README.md:4 — uncommitted "# scratch note" line, purpose unclear.
- notes.json — untracked, `{"todo": true}`, purpose unclear.
- todo_cli.py — the whole CLI: `load`/`save` JSON persistence, `add`/`list` commands.

## Session-open items
None — checked; no commitments or unfinished threads from this session.

Resume: Read ~/.claude/handoffs/demo-repo.md and continue
